import { FeatureFlagEntity } from '@/feature-flag/domain/entities/feature-flag.entity';
import { FeatureFlagMapper } from '@/feature-flag/application/dtos/feature-flag-output';

describe('FeatureFlag output unit tests', () => {
  it('should convert a featureflag in output', () => {
  });
});
